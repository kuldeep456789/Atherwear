version: '3.8'
services:
  redis:
    image: redis:alpine
    container_name: mw_redis
    ports:
      - '16379:6379'
    volumes:
      - redis_data:/data
    restart: always

volumes:
  redis_data:
